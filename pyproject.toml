[project]
name = "pederseo-server"
version = "1.0.0"
description = "Basic FastAPI server for Render deployment"
readme = "README.md"
authors = [
    { name = "Olaf Pedersen" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
